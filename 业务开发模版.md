# 任务目标
实现“账号管理界面”及其背后的“登录业务逻辑”，完成从 UI 交互到 Token 获取的全流程。

# 1. 业务逻辑实现 (核心)
请不要使用 Mock 数据，需基于我确定的方案实现真实的 `BuffUUAuthSkill`。
**方案细节：**
- **接口地址：** [例如：https://api.buff.163.com/api/login]
- **加密方式：** [例如：密码需先用 RSA 公钥加密，公钥内容为...]
- **关键参数：** [例如：需要生成 timestamp 和 sign 签名，算法为 MD5(salt + time)]
- **Token 提取：** [例如：登录成功后，从响应头 Cookie 中获取 'sessionid'，或从 body 中获取 'access_token']
- **异常处理：** [例如：遇到验证码接口返回 403 时，抛出 'NeedCaptcha' 异常]

**代码要求：**
- 将上述逻辑封装为独立服务文件 `src/services/authService.ts`。
- 暴露统一方法 `login(platform: 'BUFF' | 'UU', credentials: any): Promise<TokenInfo>`。
- 敏感信息（如私钥、Salt）必须从环境变量读取。

# 2. 界面交互实现 (UI)
**结构：**
- Steam 账号卡片 -> 下方展开平台列表 (BUFF/UU) -> 点击平台项。
**绑定逻辑：**
- 点击平台项 -> 调用 `authService.login` 方法。
- 加载中：显示 Spinner，禁用点击。
- 成功：保存 Token 到安全存储，更新 UI 状态为“已登录”，显示用户昵称。
- 失败：弹出 Toast 提示具体错误（如“密码错误”、“需要验证码”）。

# 3. 数据流与状态管理
- 定义全局 Store (如 Zustand) 存储 `authState`。
- 确保界面刷新后，能从本地存储读取 Token 并自动恢复“已登录”状态。
- 提供“退出登录”按钮，清除本地 Token 并更新 UI。

# 执行步骤
1. **第一步：** 实现 `src/services/authService.ts`，完成核心登录逻辑（请先输出代码供我确认加密部分是否正确）。
2. **第二步：** 实现 UI 组件，并绑定 `authService` 的调用。
3. **第三步：** 联调测试，确保点击后能真实获取 Token 并更新界面。

请从第一步开始，先实现业务逻辑。